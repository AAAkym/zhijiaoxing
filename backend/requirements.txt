# Flask 核心框架
Flask==2.3.3
Flask-CORS==4.0.0
Flask-SQLAlchemy==3.1.1
Flask-Caching==2.1.0
Flask-Migrate==4.0.5

# 数据库
SQLAlchemy==2.0.23
# psycopg2-binary==2.9.9  # Python 3.14 编译问题，使用 SQLite 替代

# 认证和安全
Werkzeug==2.3.7
itsdangerous==2.1.2

# 任务队列
Celery==5.3.4
redis==5.0.1

# HTTP 请求
requests==2.31.0

# 数据处理
python-dotenv==1.0.0
PyYAML==6.0.1

# 日志
python-json-logger==2.0.7

# 监控（可选）
prometheus-client==0.19.0

# Elasticsearch (可选)
elasticsearch==8.11.0

# WebSocket
flask-socketio==5.3.6
python-socketio==5.10.0

# 其他工具（使用兼容 Python 3.14 的版本）
Pillow>=10.0.0

# 文档导出
fpdf2>=2.8.0
python-docx>=1.1.0
